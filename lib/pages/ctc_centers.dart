import 'package:flutter/material.dart';

import 'orodha.dart';

class CTCCenters extends StatelessWidget {
  const CTCCenters({super.key});

  @override
  Widget build(BuildContext context) {
    return const OrodhaPage();
  }
}
